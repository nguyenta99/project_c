import dashboard from './dashboard';
import utilities from './utilities';
import history from './history';
import features from './features';

const menuItems = {
    items: [dashboard, features, history, utilities]
};

export default menuItems;
